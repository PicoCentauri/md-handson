# This is how you run the driver. You can run multiple times (but it won't be faster if you don't have multiple nodes)
i-pi-driver-py -m metatomic -a localhost -p 31415 -o 'nano_soup-00.xyz,pet-mad-s-v1.5.0-rc1.pt,cpu'
